#!/bin/bash
experiment_name=words_and_backbone
# Iterate over all job files generated by slurm_eval_wrapper.sh
for job_file in eval_${experiment_name}_*.job; do
    # Submit each job file using sbatch
    sbatch "$job_file"
done